name: v94 accident migration diff

on:
  push:
    branches:
      - v94/architecture-migration

permissions:
  contents: write

jobs:
  diff:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v94/architecture-migration
      - name: Install Playwright
        run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start server
        run: |
          python3 -m http.server 4173 >/tmp/v94-http.log 2>&1 &
          sleep 2
          curl -f http://127.0.0.1:4173/index.html >/dev/null
      - name: Capture diff
        run: node v94/browser-accident-migration-diff.mjs
      - name: Commit report
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/v94-accident-migration-diff.json
          git commit -m 'Capture accident migration diff' || exit 0
          git push origin HEAD:v94/architecture-migration